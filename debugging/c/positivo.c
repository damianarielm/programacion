#include <stdio.h>

int main() {
    int numero;
    printf("Ingrese un numero: ");
    scanf("%d", &numero);

    if (numero = 0)
        puts("El numero es 0.");
    else if (numero > 0)
        puts("El numero es positivo");
    else
        puts("El numero es negativo");
}
